version: 2.1

orbs:
  node: circleci/node@4.0.0

workflows:
  validate:
    steps:
      - checkout
      - node/install:
          install-yarn: true
      - run:
          command: yarn run lint-md
          name: Validate formatting
